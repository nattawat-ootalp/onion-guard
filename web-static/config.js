// Backend API base URL (Flask app deployed on Render). Update this after each
// Render deploy if the service's assigned hostname changes, then redeploy the
// Vercel site — nothing else in this folder needs to change.
window.API_BASE = "https://onionguard-api.onrender.com";
